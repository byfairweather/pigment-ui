{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run watch -w pigment-ui | npm run serve -w dev",
    "docs": "npm run watch -w pigment-ui | npm run serve -w docs"
  }
}
